Write the `bid()` function for a sealed-bid auction that accepts an encrypted bid amount in a confidential token. The function must use the EFFECTIVE transferred amount (silent-failure-safe), update an encrypted `_highestBid` and `_highestBidder` (eaddress) only when the new effective amount is higher, and apply transient ACL grants where appropriate. No `if`/`require` on encrypted values.
