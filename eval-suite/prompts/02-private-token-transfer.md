Write a minimal confidential ERC-7984-style token (no minting cap). Use `euint64` for balances. Expose `mint(address to, externalEuint64 enc, bytes proof)`, `confidentialTransfer(address to, externalEuint64 enc, bytes proof) returns (euint64 transferred)`, and `balanceOf(address) returns (euint64)`. Implement the silent-failure path correctly using `FHE.select` (don't revert on insufficient balance). Apply ACL on every storage write.
